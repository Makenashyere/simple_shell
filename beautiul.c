#include <stdio.h>
/**
 * main - finds sum of 2 ints
 * Return: 0
 */
int main (void)
{
	int i, j, sum;

	for (i = 0; i < 10; i++)
	{
		for (j = 10; j > 0; j--)
		{
			sum = i + j;

			return (sum);
		}
	}

	return (0);
}

